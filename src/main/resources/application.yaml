spring:
  application:
    name: Spring_Sec_Notes_App
  security:
    user:
      name: user
      password: password
  datasource:
    url: jdbc:mysql://localhost:3306/securenotes
    username: root
    password: Q!W@e3r4t5
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

logging:
  level:
    org.springframework.security: DEBUG